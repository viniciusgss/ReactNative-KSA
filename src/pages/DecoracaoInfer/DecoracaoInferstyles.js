import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  imageContainer: {
    marginTop: -40,
    height: 195,
    marginLeft: -150,
  },
  image: {
    width: '100%',
    height: '100%',
  },
});
